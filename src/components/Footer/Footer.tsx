import classes from './Footer.module.css';

const Footer = () => {
  return (
    <div className={`${classes.footer} bg-primary w-full flex justify-center items-center text-white text-sm`}>
      #The F2E 3rd Week01 #Breakfast
    </div>
  );
};

export default Footer;
